Signed project specification (agreed with supervisor)

✓ A copy of the signed specification is included in this folder as:

  Arka-B01821011_MSc Cyber Security Project specification_form_2025-26.docx

The same file should remain at the **repository root** so `scripts/dissertation_to_docx.py` can find it (see `_PROJECT_SPEC_CANDIDATES` in that script).

Word export behaviour
---------------------
The Markdown thesis (Chapter 13) lists the specification under **Appendix B**. When you run `python scripts/dissertation_to_docx.py`, the full .docx is merged **after Appendix E** under the heading **Full text — Agreed project specification (embedded)** so appendix letters A–E in the body are not duplicated.

If your filename differs, rename to match the script candidates or update `dissertation_to_docx.py`.
