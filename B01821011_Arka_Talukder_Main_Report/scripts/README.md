Experiment drivers for the dissertation now live under the repository root:

- `scripts/run_sensitivity_and_seeds.py` — sensitivity grid and multi-seed central GNN

This folder may still contain figure-generation utilities (`generate_all_figures.py`) used for the separate main-report bundle.
