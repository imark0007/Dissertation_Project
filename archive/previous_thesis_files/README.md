# Previous thesis file copies

This folder stores **older or root-level** thesis exports that were **moved** when the handoff layout was simplified:

- The **current** humanized / draft / final-Markdown exports live in `thesis_artifacts/` (see `thesis_artifacts/README.md`).
- The **old DRAFT** Word that fed the “merged prose” policy may be kept here for provenance.
- A former **`Dissertation_Arka_Talukder_Humanized.docx` at the repo root** (if present) is renamed on move to avoid clashing with the new primary path under `thesis_artifacts/`.

Do not treat files here as submission sources unless you know they are still current.
