# Optional: zip for supervisor (email)

This repo is **large**. For a **light** email you have two options.

---

## Option A — Guide only (smallest)

Zip **only** this folder:

```text
docs/viva/supervisor_share/
```

**Say in the email:** “Dr Ujjan, here are **walkthrough** instructions; the **code** and **results** are in my **main** repo **clone** / **GitHub** link.”  

They still need the **full** project for a **code** **review**.

---

## Option B — Full hand-in style bundle (matches programme)

Use **`supervisor_package/`** after **refresh** (see **`supervisor_package/README.md`**):

- Copy latest **`submission/`** Word, **`results/metrics/`**, **`results/figures/`**, **`results/alerts/`**, **`scripts/`**, **`src/`**, **`config/`**, **`SETUP_AND_RUN.md`**, **`requirements.txt`**.

Then zip **`supervisor_package/`** or the whole repo.

---

## Option C — GitHub link

If **`https://github.com/imark0007/Dissertation_Project`** is the **canonical** **remote**, send:

- Link **+** branch **`main`**,  
- plus **`docs/viva/supervisor_share/README.md`** as the **“start walkthrough”** link.

---

## Do not zip

- **`venv/`**, **`data/`** (raw CSVs often huge), **`.cursor/`**  
- Duplicate **`supervisor_share`** **inside** **`supervisor_package`** unless you want a **nested** copy (usually **not** needed)

---

Back to **[`README.md`](README.md)**
